<%= form_with(model: [:admin, document], local: true) do |form| %>
  <% if document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(document.errors.count, "error") %> prohibited this document from being saved:</h2>

      <ul>
      <% document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :slug %>
    <%= form.text_field :slug, id: :document_slug %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :document_name %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :document_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
